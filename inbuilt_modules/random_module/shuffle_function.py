# It can't be applied for set, string or tupple
# It shuffles the list according to the position and returns none
import random


let_list = [12, 13, 100, 34, 99, 67];
random.shuffle(let_list)
print(let_list)